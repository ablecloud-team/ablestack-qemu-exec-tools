# govc 환경변수 예시 (vCenter/govc 전용)
# - snapshot/inventory/CBT 관리에 사용됩니다.
# - VDDK(ESXi 데이터 전송) 인증은 별도 vddk.cred.example 파일을 사용하세요.

export GOVC_URL="https://vcenter.example.local/sdk"
export GOVC_USERNAME="administrator@vsphere.local"
export GOVC_PASSWORD="***"

# TLS 검증을 끄는 옵션 (운영 정책에 맞춰 0/1 선택)
export GOVC_INSECURE=1
